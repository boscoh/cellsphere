{
  "name": "cellsphere",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/gen-params-table.mjs --check && vite build",
    "preview": "vite preview",
    "test": "npm run test:tail && npm run test:headless",
    "test:tail": "node scripts/tail-equivalence.mjs",
    "test:headless": "node scripts/headless-sim.mjs"
  },
  "dependencies": {
    "three": "^0.185.1",
    "vue": "^3.5.41"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.2.2"
  }
}
